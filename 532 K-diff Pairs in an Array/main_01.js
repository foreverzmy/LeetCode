/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var findPairs = function(nums, k) {
  let dict = {};
  let count = 0;
  for (let i = 0; i < nums.length; i++) {
    const tmp = nums[i];
  }
};
